import 'package:flutter/material.dart';

class APIOfflinePage extends StatelessWidget {
  const APIOfflinePage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Text('API OFF LINE SORRY'),
      );
}
